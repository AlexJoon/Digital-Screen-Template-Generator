[build]
builder = "nixpacks"

[deploy]
startCommand = "python -c 'import main' && uvicorn main:app --host 0.0.0.0 --port 8000 --timeout-keep-alive 120"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
